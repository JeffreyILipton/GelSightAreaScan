function test(deltas)
% Use this function to actively access the data inputted
% from the capture function "deltas" (the force data).

% Print out the force data to the command window
% The following can be commented out and replaced with
% "real" code. 
fprintf('Delta is %d\n', deltas);